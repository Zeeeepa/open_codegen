# Unified AI Provider Gateway - Complete Dependencies
# This file includes ALL dependencies needed by all 15 API services

# Core Flask web framework (for main.py)
Flask==3.0.0
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3

# HTTP client libraries (essential for all APIs)
requests==2.31.0
httpx==0.25.2
aiohttp==3.9.1
curl_cffi==0.6.2

# Configuration and environment
python-dotenv==1.0.0
pyyaml==6.0.1

# Process management
psutil==5.9.6

# JSON and data handling
pydantic==2.5.0
python-multipart==0.0.6

# API-specific dependencies (from analyzing the API repositories)

# For grok2api (requires curl_cffi and web scraping)
fake-useragent==1.4.0
beautifulsoup4==4.12.2
lxml==4.9.3

# For k2think APIs (OpenAI compatibility)
openai==1.3.7
anthropic==0.7.8

# For Z.ai APIs (WebSocket and SSE support)
websockets==12.0
sse-starlette==1.6.5

# For FastAPI-based APIs
fastapi==0.104.1
uvicorn==0.24.0
starlette==0.27.0

# Crypto and security (for authentication)
cryptography>=41.0.0
PyJWT==2.8.0

# Data processing (some APIs need this)
pandas==2.1.4
numpy==1.25.2

# Database support (lightweight)
sqlalchemy==2.0.23

# Additional utilities
click==8.1.7
rich==13.7.0
tqdm==4.66.1
colorlog==6.8.0

# Testing and development
pytest==7.4.3
pytest-asyncio==0.21.1

# Remove problematic dependencies that cause conflicts
# codegen>=0.1.0  # This causes issues, we'll handle Codegen separately
# codegen-api-client>=0.1.0  # This causes issues
